package Links;

public class CompPair {
    //method used to determine if both roots are complex
     String checker(double num_real, double num_fake){
        return num_real == num_fake ? "They are identical": "They are different";
    }
}
